{
  "name": "helpcenteriq",
  "private": true,
  "version": "0.1.0",
  "description": "HelpCenterIQ — AI-powered knowledge gap intelligence for Zendesk.",
  "workspaces": [
    "server",
    "dashboard"
  ],
  "scripts": {
    "dev:server": "npm run dev --workspace=server",
    "dev:dashboard": "npm run dev --workspace=dashboard",
    "lint": "npm run lint --workspace=server && npm run lint --workspace=dashboard",
    "test": "npm test --workspace=server"
  }
}
