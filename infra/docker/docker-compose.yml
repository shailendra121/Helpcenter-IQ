services:
  postgres:
    image: pgvector/pgvector:pg16
    environment:
      POSTGRES_USER: helpcenteriq
      POSTGRES_PASSWORD: helpcenteriq
      POSTGRES_DB: helpcenteriq
    ports:
      - "5432:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata:
